import React, { useState } from "react";
import {
  EmailShareButton,
  FacebookShareButton,
  LinkedinShareButton,
  RedditShareButton,
  TwitterShareButton,
  WhatsappShareButton,
} from "react-share";

export default function individualVoiceBlast(){
	return(
        <div>
        	// audio player
        	// audio tract title
        	// duration
        </div>
		);
}